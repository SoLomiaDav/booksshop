@model Book

<div class="col-lg-4">
    <img class="img-trumbnail" src="@Model.img" alt="@Model.name" />
    <h2>Назва: @Model.name</h2>
    <p>@Model.author</p>
    <p>Ціна: @Model.price.ToString("c")</p>
    <p><a class="btn btn-warning" href="#">Більше</a></p>

</div>